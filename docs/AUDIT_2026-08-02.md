# Audit — 2026-08-02

Run after Tier 0–2, per the sequencing in `PRIORITIES_2026-08.md`: the workbench
being usable changes what is worth auditing, and the notation decision changes
what the docs should say.

Findings are ranked by whether they can bite someone, not by how much work they
are. Several of the things carried in the backlog turned out to be smaller or
already-correct than the backlog claimed — those are recorded too, because a
backlog that overstates its debts is its own problem.

---

## Fixed in this pass

### A1. Three apps shipped no cache-bust

`serpe`, `pitchfold` and `drawnqurve` loaded `bundle.js` with no version, so a
rebuild kept serving the browser's cached copy and a landed fix looked like it
had not landed. Vane already stamped a build id; Workspace got one on 08-02
after this exact papercut cost a debugging detour, and Alex had already hit it
once by hand ("Had to do a new hard refresh").

Now stamped the same way in all three. Verified by loading Serpe:
`bundle.js?v=2026-08-02.202155Z`, arcs render, no console errors.

`MIDIcurator`, `PickPCS`, `chord-dictionary` and `progression-studio` load
`src/main.jsx` through a dev server rather than a built bundle, so the question
does not arise for them.

---

## Open, ranked

### B1. `accompany --rhythm` rejects poly notation *(real dead-end)*

`packages/accompaniment/src/pipeline.ts:144` calls `parseUPI` on `opts.rhythm`,
which comes straight from `msuite accompany --rhythm "<UPI>"` — user input. A
lane-separated rhythm throws `--rhythm "…" did not parse as UPI`, for notation
the rest of the suite plays.

The layering rule is *user input → `parsePolyUPI`; library internals only →
`parseUPI`* (SERPE_DAW_FINDINGS F7). This is the one remaining violation that a
person can reach by typing.

**Recommended:** parse with `parsePolyUPI` and use lane 1, saying so — a
harmonic overlay on a bass line is a single rhythm, so taking lane 1 is honest,
and the current behaviour rejects rather than explains.

### B2. Serpe's library loses the reading for poly patterns *(minor)*

`apps/serpe/main.jsx:1332` and `:1382` parse saved library items with
`parseUPI`. A saved poly pattern gets no `k/n` reading and no long/short foot —
it degrades to nothing rather than crashing, so this is cosmetic, but it is user
data going through the mono parser.

**The other three Serpe call sites are correct and should be left alone:**
`:744` is explicitly guarded (`No '/' in this string, so we have just left
poly`), and `:762` / `:916` are `parseBase` callbacks for `progressiveAt`, whose
base is a single body by definition. The backlog carried these as "×3
unreviewed"; they are reviewed now, and they are right.

### B3. PickPCS has no accessibility markup at all

Counting `aria-label` / `role=` across each app's own source:

```
MIDIcurator 205    workspace 58    serpe 23    exquisite-fingerings 20
vane 17            progression-studio 9        pitchfold 7
style-gallery 4    drawnqurve 1    chord-dictionary 1    PickPCS 0
```

**PickPCS is zero across nine files.** `chord-dictionary` and `drawnqurve` are
effectively zero too.

This also corrects the backlog: it listed `workspace` as the unaudited one, and
Workspace is among the best-covered apps in the suite. The gap is elsewhere.

INTENT B6 frames accessibility as welcome rather than compliance, which makes an
app with no labels at all the sharpest version of the problem: a pitch-class set
is a diagram, and a diagram with no text is a closed door.

### B4. `*N` lengthening: C++ and JS now generate different material

Resolved on the JS side 2026-08-02 — `polyLaneAt` seeds from the base pattern,
so a trigger names a pattern. The C++ engine still grows `*N` with its own RNG.

D3 means the engine wins where it is present, so nothing is *broken*: a plugin
session is self-consistent and a standalone session is self-consistent. But the
same notation gives different material in the two places, and the conformance
vectors cannot pin `*N` until they agree.

**Needs a decision, not a fix:** should the seed cross over? If yes, it is a C++
change plus a vector regeneration.

### B5. `msuite jam` is promised in six documents and does not exist

`DESIGN_BRIEF`, `MASTER_PLAN`, `INTENT` (H1), `PRIORITIES`, `TRAINING_PLAN`,
`TESTING_NOTES` — and three of those are already *about* it not existing, which
is its own kind of tell. `TESTING_NOTES` puts it plainly: "The name has outlived
several answers."

Tier 0 changed the calculus. Workspace now boots as Pattern → Player → Vane
Synth, plays poly, advances progression and has a drum kit — which is most of
what `jam` was ever described as doing, in a form that already exists and is
already tested.

**Recommended: retire the name.** One line in INTENT H1 recording that Workspace
absorbed it, and remove the forward-looking promises from the other five. A
promise repeated in six places and kept in none is worse than an absence — and
the suite's own rule is that a dead-end is a bug.

---

## Worth adding, from looking around

Not requests, just what the sweep suggested:

- **A drum-kit page for the site.** `@enkerli/drumsynth` is eight sounds with
  audible parameters and no licensing story to explain. It is the easiest thing
  in the suite to demonstrate in a browser, and nothing currently shows it.
- **`msuite upi --wav` deserves to be findable.** It is the fastest path from a
  notation to a sound anywhere in the suite and it is documented only in
  `--help`.
- **The articulation examples want a page, not a folder.** Nine `.mid`, nine
  `.wav` and a README is the right *content* and the wrong *surface* for
  something whose whole point is that you can hear the difference.

---

## Verification

`npx vitest run` — 1749 passing. Serpe, PitchFold and DrawnQurve rebuilt and
Serpe loaded in a browser after the cache-bust change.

Nothing in B1–B5 was changed. They are findings, and three of them (B1's
approach, B4, B5) want Alex's call rather than a patch.
