{
  "name": "serpe-webui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npx esbuild main.jsx --bundle --conditions=source --inject:react-globals.js --loader:.css=text --loader:.svg=dataurl --banner:js='/*! Bundles WEBMIDI.js (c) Jean-Philippe Cote, Apache-2.0, https://webmidijs.org - see @enkerli/webmidi/THIRD_PARTY_LICENSES.md */' --define:process.env.NODE_ENV=\\\"production\\\" --minify --outfile=dist/bundle.js && cp index.html dist/index.html && mkdir -p dist/fonts && cp ../../packages/ui/fonts/*.woff2 dist/fonts/",
    "dev":   "npx esbuild main.jsx --bundle --conditions=source --inject:react-globals.js --loader:.css=text --loader:.svg=dataurl --define:process.env.NODE_ENV=\\\"development\\\" --sourcemap --outfile=dist/bundle.js && cp index.html dist/index.html && mkdir -p dist/fonts && cp ../../packages/ui/fonts/*.woff2 dist/fonts/",
    "watch": "npx esbuild main.jsx --bundle --conditions=source --inject:react-globals.js --loader:.css=text --loader:.svg=dataurl --define:process.env.NODE_ENV=\\\"development\\\" --sourcemap --watch --outfile=dist/bundle.js"
  },
  "dependencies": {
    "@enkerli/ui": "*",
    "@enkerli/upi": "*",
    "@enkerli/webmidi": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
